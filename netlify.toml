# ==========================================
# 📁 netlify.toml
# CONFIGURATION NETLIFY OPTIMISÉE POUR ÉVITER TIMEOUT
# ==========================================

[build]
  # Répertoire de base pour le build
  base = "react-app"
  
  # ✅ COMMANDE BUILD CORRIGÉE (forcer installation devDependencies)
  command = "NODE_ENV=development npm install --prefer-offline --no-audit --progress=false && npm run build"
  
  # Répertoire à publier (CORRECTION: chemin simple)
  publish = "dist"

# ✅ VARIABLES D'ENVIRONNEMENT CORRIGÉES
[build.environment]
  # Optimisations npm
  NPM_CONFIG_PROGRESS = "false"
  NPM_CONFIG_AUDIT = "false"
  NPM_CONFIG_FUND = "false"
  
  # Optimisations Vite
  VITE_BUILD_TARGET = "es2020"
  GENERATE_SOURCEMAP = "false"
  CI = "true"
  
  # Désactiver fonctionnalités non nécessaires
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"

# ✅ CONTEXTE PRODUCTION CORRIGÉ (dev install puis prod build)
[context.production]
  command = "NODE_ENV=development npm install --prefer-offline --no-audit --progress=false && NODE_ENV=production npm run build"
  
[context.production.environment]
  NODE_ENV = "production"

# ✅ CONTEXTE DEPLOY-PREVIEW CORRIGÉ
[context.deploy-preview]
  command = "NODE_ENV=development npm install --prefer-offline && npm run build"

# ✅ BRANCH DEPLOY CORRIGÉ  
[context.branch-deploy]
  command = "NODE_ENV=development npm install --prefer-offline && npm run build"

# ✅ REDIRECTIONS SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

# ✅ HEADERS OPTIMISÉS
[[headers]]
  for = "/*"
  [headers.values]
    # Sécurité de base
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff" 
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
    
    # PWA
    Service-Worker-Allowed = "/"
    
    # CORS minimal pour Firebase
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, POST, PUT, DELETE, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type, Authorization"

# ✅ CACHE AGRESSIF POUR ASSETS
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
    
# ✅ CACHE SERVICE WORKER
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

# ✅ CACHE MANIFEST PWA
[[headers]]
  for = "/manifest.json"
  [headers.values]
    Cache-Control = "public, max-age=86400"

# ✅ FONCTIONS NETLIFY (si nécessaire plus tard)
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# ✅ OPTIMISATIONS DE BUILD
[build.processing]
  skip_processing = false

[build.processing.css]
  bundle = true
  minify = true

[build.processing.js]
  bundle = true
  minify = true

[build.processing.html]
  pretty_urls = true
